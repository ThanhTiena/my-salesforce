{
  "name": "mongo-proxy",
  "version": "1.0.0",
  "description": "MongoDB REST proxy — replaces deprecated Atlas Data API for Salesforce callouts",
  "main": "src/handler.js",
  "scripts": {
    "test":    "jest --coverage",
    "package": "npm ci --omit=dev && zip -r proxy.zip src node_modules package.json"
  },
  "dependencies": {
    "mongodb": "^6.0.0"
  },
  "devDependencies": {
    "jest": "^29.0.0"
  },
  "jest": {
    "testEnvironment": "node",
    "testMatch": ["**/test/**/*.test.js"]
  }
}
