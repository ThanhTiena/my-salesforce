<?xml version="1.0" encoding="UTF-8"?>
<NamedCredential xmlns="http://soap.sforce.com/2006/04/metadata">
    <label>AI MongoDB Vercel Proxy</label>
    <!--
        Replace YOUR_VERCEL_DOMAIN with your actual Vercel project domain.
        Example: https://my-salesforce-proxy.vercel.app

        Authentication is handled via the x-api-key header injected from the
        External Credential Principal — the key must match SALESFORCE_API_KEY in Vercel.

        Endpoints used:
          POST /api/salesforce/logs      — write audit log entry
          POST /api/salesforce/records   — upsert record
          GET  /api/salesforce/records   — query records
    -->
    <endpoint>https://YOUR_VERCEL_DOMAIN.vercel.app</endpoint>
    <principalType>NamedUser</principalType>
    <protocol>NoAuthentication</protocol>
    <allowMergeFieldsInHeader>true</allowMergeFieldsInHeader>
    <allowMergeFieldsInBody>false</allowMergeFieldsInBody>
    <generateAuthorizationHeader>false</generateAuthorizationHeader>
</NamedCredential>
