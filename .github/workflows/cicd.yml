# ARCHIVED - see validate-pr.yml, deploy-sandbox.yml, deploy-production.yml
name: _archived_cicd
on: workflow_dispatch
jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is archived. Use validate-pr.yml or deploy-sandbox.yml."
